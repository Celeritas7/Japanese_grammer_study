// TODO: Extract progressive-reveal flashcard from prototype-v2.jsx
// Shows card front → progressive hints → marking panel

export default function FlashCard({ card, onMark }) {
  // Migrate from prototype-v2.jsx → the flashcard rendering in Flashcards component
  return <div>FlashCard — migrate from prototype</div>;
}
