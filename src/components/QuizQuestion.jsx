// TODO: Extract quiz question display from prototype-v2.jsx
// Renders a single question with answer options and feedback

export default function QuizQuestion({ question, onAnswer, selectedAnswer }) {
  // Migrate from prototype-v2.jsx → the question rendering in Quiz component
  return <div>QuizQuestion — migrate from prototype</div>;
}
