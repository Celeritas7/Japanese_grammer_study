// TODO: Extract expandable grammar card from prototype-v2.jsx
// Used in list view of GrammarPage

export default function GrammarCard({ grammar, expanded, onToggle, groupLabel }) {
  // Migrate from prototype-v2.jsx → the list view card in GrammarReference
  return <div>GrammarCard — migrate from prototype</div>;
}
